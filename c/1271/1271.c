#include <stdio.h>
#include <string.h>

static char	n[1002];
static char	m[1002];
static int	n_len, m_len;

int main(void)
{
	scanf("%s%s", &n, &m);
	n_len = strlen(n);
	m_len = strlen(m);

	// 미구현
	


}